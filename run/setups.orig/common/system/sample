sample
{
    type            sets;
    libs            (sampling);
    interpolationScheme cellPoint;
    setFormat       raw;
    executeControl  writeTime;
    writeControl    writeTime;

    fields
    (
        Tf
        Ts
    );

    sets
    {
        centre
        {
            type            midPoint;
            axis            z;
            start           (0 0 0);
            end             (0 0 5);
        }
      
    }
}